# AI 工作原则（Trellis 兼容全局模板）

> 用于全局 `AGENTS.md`。优先级：
>
> **用户明确指令 > 项目内更近的 `AGENTS.md` > 当前 Trellis Task/PRD/Design > 本文件。**
>
> 本文件只定义稳定的全局原则、工作流路由和验证边界。项目专属业务规则、测试命令和实现细节放在项目级配置中。

## 1. 基本原则

* 默认使用中文回复；存在不确定性时明确说明。
* 不虚构事实，不声称执行过实际未执行的测试、部署、提交、推送或工具调用。
* 优先简单、稳定、可维护的方案。
* 只修改完成当前任务必要的内容；不顺手重构、格式化或优化无关代码。
* 保留用户已有修改，不擅自覆盖或回滚无关工作区变化。
* 修改前先理解现有实现。
* Skill、MCP、Agent 和工作流用于降低实际风险，**不为了调用而调用**。

---

## 2. 工作流路由

### 项目存在 `.trellis/`

Trellis 是任务、规格和状态的唯一工作流来源。

收到开发、修复或实施请求时，主动读取 `.trellis/workflow.md`，确认当前会话关联的 Task、状态及相关规格，不等待用户点名 Trellis。只复用与本次需求匹配的 Task，不把任意未归档任务当作当前任务。

没有匹配 Task 时，按项目规定进入建任务流程；需要建任务同意时只确认该事项，已有明确授权不重复询问。纯问答或只读咨询不自动创建任务。

有可用的 Trellis 命令或 Skill 时优先使用；未暴露时使用 `.trellis/scripts/` 中的原生脚本或执行等价步骤，并说明降级。没有专用工具不等于跳过 Trellis。

轻量化只减少不必要的规划、访谈与验证，不省略项目要求的任务记录和状态管理。

#### 简单且明确

例如局部 Bug、小范围 UI、配置、单模块逻辑修改：

* 进入 Trellis 轻量流程，记录必要的需求与验收项，满足项目实施条件后直接实施；
* 做受影响范围的针对性验证；
* 不自动启动复杂规划或全仓验证。

#### 需要升级流程

以下情况根据实际风险升级：

* 存在影响正确实现的关键需求歧义；
* 新增或修改跨模块业务契约；
* 公共 API、数据模型或持久化结构变化；
* migration、删除、兼容性、安全等高风险修改；
* 需要形成新的领域或架构决定。

复杂、跨模块、需求不明确或涉及上述高影响变更时，主动读取并使用 `$grill-with-docs`，在 Trellis Phase 1.1 审查需求完整性。

使用 Skill 与向用户提问分开判断：先查现有 PRD、规格、代码和历史决定；只有存在影响范围、业务规则、验收或高风险取舍且需要用户决策的未决项时，才逐个提问。需求已经完整时记录审查结论并继续，不重复访谈。

已有充分的 Phase 1.1 结论且本次范围未变化时复用，不因继续会话、增加提交或进入验证阶段重新启动 Grill。

不创建第二套 Task、PRD、Design 或 Spec。

使用 Grill with Docs 后不再运行 `trellis-brainstorm`。

### 项目不存在 `.trellis/`

使用项目已有工作流。

不得仅因为本文件而自动：

* 初始化 Trellis；
* 引入 Spec 框架；
* 创建第二套任务系统。

---

## 3. Skill 使用原则

Skill 是按需能力，不是固定流水线。

### Grill with Docs

作为复杂任务在 Trellis Phase 1.1 的需求审查与访谈能力，按第 2 节主动触发。

简单且明确的任务不强制加载。复杂任务先审查，按未决项决定是否访谈；不得把“无需提问”解释为“无需审查”。如果 Skill 文案要求机械提问或重复确认，按本文件的审查与提问边界执行，仍遵守项目明确要求的授权门槛。

开始使用时简要说明用途；审查后说明存在的未决项或可以继续的结论，不以声称“已调用”替代实际读取和审查。

### Graphify

已有可用图谱时，可用于复杂架构、关系、数据流和陌生路径探索。

简单源码定位或局部修改不要求使用。

不得为了任务自动生成/更新图谱、安装 hook 或提交图谱产物。

### GitNexus

主要用于：

* 公共接口；
* 数据契约；
* 跨模块高影响修改；
* 删除或迁移；
* 陌生高风险调用链。

局部低风险修改不自动调用。

### Karpathy Guidelines

作为编码和审查的横切原则。

不作为每次修改都必须单独执行的工作流阶段。

### TDD

适用于需要自动化测试证明的行为变化。

优先用于：

* 新业务行为；
* 适合回归测试锁定的 Bug；
* 高风险核心逻辑。

不机械要求所有文案、配置、类型调整或极小修改执行完整 RED → GREEN → REFACTOR。

即使使用 TDD，也只运行当前行为相关测试。

### 其他 Skill

Diagnosing Bugs、Codebase Design、Resolving Merge Conflicts、Release、Memory 等仅在当前任务确有需要时使用。

Skill 不得创建与 Trellis 平行的任务生命周期。

---

## 4. 风险与验证范围

验证范围按照实际影响升级，而不是按照“修改了代码”升级。

### L0：非运行时

文档、注释、文案等。

只做必要的文件/格式检查。

### L1：局部修改

单文件、单模块、局部 Bug、内部实现。

执行相关的最小检查。

### L2：跨模块

内部接口、多模块调用、完整业务链。

验证受影响模块及必要集成路径。

### L3：高影响

公共 API、数据库、安全、权限、构建系统、共享数据契约、迁移等。

允许扩大影响分析和验证范围。

始终遵循：

> **选择能够对当前修改提供充分信心的最小验证范围。**

---

## 5. 实现阶段验证

实现过程中使用：

> **最小必要验证 + 按风险逐级升级。**

相关连续修改可视为一个实现批次。

优先：

1. 完成一个逻辑批次；
2. 运行受影响范围的针对性检查；
3. 通过后继续实施。

不要求每修改一个文件、函数或 diff 都立即运行测试。

验证升级顺序：

1. 文件/静态检查；
2. 相关单元测试；
3. 受影响模块测试；
4. 必要集成测试；
5. 项目级/全仓检查。

只有前一级不足以建立合理信心时才升级。

### 实现阶段默认禁止

除非用户、项目规则或实际风险明确要求，否则不要因为以下原因自动运行全仓验证：

* 修改了源码；
* 修改文件数量增加；
* 出现新 commit；
* 一个局部测试失败；
* 刚修复一个问题；
* “为了保险”；
* 任何行为变化。

不得形成：

> 修改 → 全量测试 → 修改 → 全量测试

的循环。

局部检查失败时：

1. 定位问题；
2. 修复；
3. 重跑对应最小检查。

不要因为一次局部失败直接升级为全仓复测。

---

## 6. 可执行工作流门禁

当 `config/workflow_check.py` 可用时，Codex、Cursor、Claude 共用：

`python3 config/workflow_check.py --project-root "$PWD" ...`

helper 只负责工作流门禁和质量证据，不替代 Trellis 状态机或项目原生检查。

### readiness

复杂任务在 `task.py start` 前：

`readiness --task <任务目录> --complex`

轻量任务：

`readiness --task <任务目录>`

`readiness` 只检查规划工件和上下文，不修改任务状态。

---

## 7. 最终验证

只有当实现基本完成、不会继续常规功能修改时，才进入最终验证。

最终验证是**任务级门禁**，不是代码修改级门禁。

执行：

1. 必要针对性检查；
2. 当前 Task 要求的最终原生 `trellis-check`；
3. 运行一次：

`quality --task <任务目录>`

`quality` 会执行所选检查并记录结果，不是仅登记先前结果。应在最终批次统一安排，避免先手动运行同一套命令、随后又由 `quality` 重复运行。

当前 Task 应列明所选检查及覆盖范围；可用命令清单不是默认必跑清单。文件检查足以覆盖的文档修改，不自动选择包级全量质量配置。

AI-workflow 包可使用内置质量配置。

其他项目必须提供真实检查命令：

`--check <名称>=<实际检查命令>`

没有真实检查命令时不得生成虚假质量证据。

CI 执行 `quality` 时不传 `--task`，不得依赖活动任务。

### quality 重跑

同一任务、同一最终验证批次默认只运行一次。

以下变化本身不要求扩大验证；是否复用证据，以检查覆盖范围及 helper 校验结果为准：

* Git HEAD；
* commit hash；
* commit 数量；
* Task/Journal 状态；
* 不影响质量证据的说明性内容。

成功证据仅在其覆盖内容发生实质变化并失效时重跑相应最终检查。若上次检查失败，则集中修复后统一重试，不要求先有成功证据。

证据复用必须以 helper 的实际校验结果为准。若 helper 按整仓内容计算指纹，文档变化也可能使证据失效；应说明覆盖粒度限制，不得跳过失败门禁或手改指纹。重新验证仍选择最小充分范围，不自动升级为全仓测试。

判断原则：

> **不是仓库变化就重跑，而是已有最终质量证据失效才重跑。**

若最终检查发现多个问题：

1. 集中修复；
2. 做针对性验证；
3. 修复批次完成后统一重跑一次必要的最终门禁。

禁止：

> 修一个问题 → quality → 再修一个 → quality

---

## 8. Multi-agent

启用 `multi_agent` 时：

* 子代理服从用户指令、项目 `AGENTS.md`、当前 Trellis Task 和本文件；
* 子代理只处理分配范围；
* 子代理只运行自己范围内必要的针对性检查；
* 不默认运行全仓测试、最终 `quality` 或 `completion`；
* 多个子代理不得重复执行相同完整验证；
* 主代理负责汇总，并执行任务级最终门禁。

只有主代理明确委派时，子代理才执行项目级完整检查。

---

## 9. 记忆

Recallium、Codex memories、Memory Skill、Mem0 等属于长期上下文能力。

不得替代：

* Trellis PRD；
* Task；
* Design；
* Journal；
* 正式项目规格。

任务依赖历史决策时先检索再回答。

工具不可用时明确降级，不虚报已检索或已写入。

---

## 10. Git

未经明确要求，不执行：

* commit；
* push；
* merge；
* rebase；
* 创建 PR；
* 删除分支；
* force push。

避免可能破坏未提交工作的：

* `git reset --hard`；
* `git clean`；
* 类似破坏性操作。

完成任务后简要说明：

* 修改内容；
* 实际运行的验证；
* 验证结果；
* 未验证内容或已知限制。

不得把未执行的检查描述为已通过。

---

## 11. completion

归档前运行：

`completion --task <任务目录>`

优先复用已有质量证据，并确认：

* 验收项已完成；
* 所需证据存在；
* 内容指纹仍有效。

仅 Git HEAD、commit 数量或 hash 改变，而受验证内容未变化时，不重新执行完整验证。

---

## 12. 配置边界

* 不假定存在 Trellis MCP。
* 不自动修改全局 `config.toml`、MCP、插件、hooks 或工作流配置。
* 只有用户明确授权时才做必要的增量调整。
* helper / Skill / MCP 不可用时采用最小合理降级方案，并明确说明。
* 不得声称不存在的工具已经运行。

---

## 核心决策原则

**验证：**

> 使用能够证明当前修改正确性的最小充分验证范围。

**Skill：**

> 只有能够明显降低当前任务风险或提高正确性时才调用。

**全量测试：**

> 不因为“修改了代码”自动运行，只因为影响范围确实需要。

**最终门禁：**

> 任务实现稳定后集中执行，而不是每次修改后执行。

**重新验证：**

> 只有已有质量证据因后续实质修改失效时才重新执行。

**总原则：**

> 不为了流程完整而增加没有实际收益的 Skill、Agent、工具调用、测试或工作流步骤。
