# Implement: ps-install-components

1. Confirm `install-lib.ps1` available.
2. Port four component scripts; keep side-by-side with `.sh`.
3. Smoke: dry-run + copy to temp targets on Windows.
4. Quality check for this child scope.

Depends: `07-27-ps-install-lib`.
