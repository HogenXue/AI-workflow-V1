# Implement: ps-install-entry

1. Implement `install.ps1` dispatch + interactive + profiles calling component scripts.
2. Implement `install.cmd` launcher with missing-pwsh error.
3. Smoke non-TTY no-args exit 2; `install.cmd skills --dry-run`.

Depends: components + merge scripts present.
