# 证据闭环

调试按以下顺序推进：先记录可重复的输入、环境、版本和预期行为；然后收集实际输出、错误、日志或测试失败；再从现象沿调用路径缩小到可证实的根因。不能稳定复现时，结论只能是待确认，不能直接修改推测中的模块。

修复应限制在根因与必要回归保护范围内。每一项验证要说明命令或操作、结果和覆盖边界。日志、调试器、性能分析器、远端监控或 CI 不可用时，可使用本地测试、代码阅读、`rg` 与只读 Git 检查替代，但必须标记未覆盖的能力。
