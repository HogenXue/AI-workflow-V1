# 修复运行时错误

## 用户请求

“服务重启后下单接口返回 500，请定位并修复；不要顺带改无关流程。”

## 工作流

1. 合并 `config/defaults.yaml` 与目标仓库的 `hogen-codex.yaml`，确认项目覆盖项。
2. 用同一请求、环境和数据尝试复现，收集错误日志、请求参数和相关调用路径。
3. 仅在证据足以隔离根因后提出最小修复，并重跑对应接口或测试。
4. 调试器、日志平台或远端环境不可用时，明确降级为本地日志、测试、代码阅读、`rg` 和只读 Git 检查。

## 最终报告

已执行：使用给定请求复现失败，并从本地堆栈定位到空值校验缺失；已添加最小保护并通过对应回归测试。未执行：未访问生产日志平台，因此未确认生产历史请求的发生比例。若未能稳定复现，则只能报告现有证据和下一步采集条件，不能声称根因已定位。
