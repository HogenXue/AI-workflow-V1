interface:
  display_name: "Grill Me"
  short_description: "Clarify complex requirements before OpenSpec creates the Spec."
  default_prompt: "Use $grill-me to clarify this complex change before OpenSpec writes its Spec."
policy:
  allow_implicit_invocation: true
