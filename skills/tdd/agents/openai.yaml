interface:
  display_name: "TDD"
  short_description: "以可验证的 RED、GREEN、REFACTOR 循环实现功能与缺陷修复"
  default_prompt: "Use $tdd to implement this behavior with a verified red-green-refactor cycle."
policy:
  allow_implicit_invocation: true
