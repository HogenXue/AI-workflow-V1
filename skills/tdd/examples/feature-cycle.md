# 示例：库存不能扣成负数

Trellis PRD 已确认：库存为 1 时购买 1 件后为 0；库存为 0 时购买必须被拒绝。

1. **RED**：先写“库存为 0 时拒绝购买”的测试并运行，确认它因当前实现允许扣减而失败。
2. **GREEN**：仅增加扣减前的库存检查，运行该测试和库存模块相邻测试，全部通过。
3. **REFACTOR**：若已有同类校验，提取共享校验函数并重跑测试。
4. 在 Trellis Journal 写入测试命令和通过结果；不把场景复制为第二份 PRD 或 Spec。
