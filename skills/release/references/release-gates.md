# 发布门禁

发布前明确版本、变更范围、目标环境、负责人、批准条件和用户影响。执行可用的构建、测试、迁移/兼容性检查，并将失败、跳过和未覆盖项写入发布记录。没有明确授权时，只能准备和验证，不能部署、切流或创建发布。

每次发布必须有可操作的回滚条件、命令或步骤，以及验证回滚有效性的责任人。发布后检查服务健康、核心流程、错误率或告警；若监控、部署平台或生产访问不可用，只能报告本地或配置层证据，不能替代生产确认。
