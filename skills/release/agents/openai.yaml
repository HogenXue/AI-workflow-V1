interface:
  display_name: "Release Readiness"
  short_description: "Plan release gates, rollback, rollout, and status."
  default_prompt: "Use $release to plan and verify this release, confirm rollback readiness, execute only authorized steps, and separate completed from unexecuted checks."
policy:
  allow_implicit_invocation: true
