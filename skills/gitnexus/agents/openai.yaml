interface:
  display_name: "GitNexus Impact"
  short_description: "Analyze code impact, worktrees, and commit scope before changes."
  default_prompt: "Use $gitnexus to assess symbol impact, worktree state, and affected flows before changing code."
policy:
  allow_implicit_invocation: true
